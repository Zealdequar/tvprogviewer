@model TvChannelDetailsModel
@if (Model.EmailAFriendEnabled)
{
    <div class="email-a-friend">
        <button type="button" class="button-2 email-a-friend-button" onclick="setLocation('@Url.RouteUrl("TvChannelEmailAFriend", new { tvchannelId = Model.Id })')">@T("TvChannels.EmailAFriend")</button>
    </div>
}